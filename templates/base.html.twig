<!DOCTYPE html>
<html lang="fr">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1">
    <title>{% block title %}Welcome!{% endblock %}</title>
    <link rel="icon" href="...">

    {% block stylesheets %}
        <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.1.3/dist/css/bootstrap.min.css" rel="stylesheet">
        <link rel="stylesheet" href="{{ asset('assets/styles/app.css') }}">
        <link href="https://fonts.googleapis.com/css?family=Open+Sans:400,700" rel="stylesheet">
        <link rel="stylesheet" href="https://cdnjs.cloudflare.com/ajax/libs/font-awesome/6.3.0/css/all.min.css">
    {% endblock %}
</head>
<body class="d-flex flex-column min-vh-100">
{% block header %}

<!-- NAVBAR -->
<nav class="navbar navbar-expand-lg navbar-dark bg-dark d-none d-md-flex">
    <div class="container-fluid px-4 px-lg-5">
        <div class="collapse navbar-collapse" id="navbarSupportedContent">
            <ul class="navbar-nav ms-auto mb-2 mb-lg-0 d-flex align-items-center gap-3">
                <li class="nav-item">
                    <a class="nav-link" href="{{ path('app_index') }}">
                        <i class="fa-solid fa-house me-1"></i>Accueil
                    </a>
                </li>


                {% if app.user %}
                    <li class="nav-item">
                        <a class="nav-link" href="{{ path('app_index') ~ '#tricksContainer' }}">
                            <i class="fa-solid fa-user me-1"></i>Tricks
                        </a>
                    </li>
                    <li class="nav-item">
                        <a class="nav-link text-danger" href="{{ path('app_logout') }}">
                            <i class="fa-solid fa-right-from-bracket me-1"></i>Déconnexion
                        </a>
                    </li>
                {% else %}
                    <li class="nav-item">
                        <a class="nav-link" href="{{ path('app_login') }}">
                            <i class="fa-solid fa-right-to-bracket me-1"></i>Connexion
                        </a>
                    </li>
                    <li class="nav-item">
                        <a class="nav-link" href="{{ path('app_register') }}">
                            <i class="fa-solid fa-user-plus me-1"></i>Inscription
                        </a>
                    </li>
                {% endif %}
            </ul>
        </div>
    </div>
</nav>
{% endblock %}


<!-- CONTENU DE PAGE -->
<main class="flex-grow-1">
    {% block body %}{% endblock %}
</main>

{# NAV MOBILE #}
<nav class="navbar navbar-dark bg-dark  d-md-none">
    <div class="container d-flex justify-content-around">
        <a class="nav-link text-white" href="{{ path('app_index') }}">
            <i class="fa-solid fa-house fa-lg"></i>
        </a>
        {% if app.user %}
            <a class="nav-link text-white" href="{{ path('app_index') ~ '#tricksContainer' }}">
                <i class="fa-solid fa-bookmark fa-lg"></i>
            </a>
            <a class="nav-link text-white" href="{{ path('app_logout') }}">
                <i class="fa-solid fa-right-from-bracket fa-lg"></i>
            </a>
        {% else %}
            <a class="nav-link text-white" href="{{ path('app_register') }}">
                <i class="fa-solid fa-user-plus me-1 fa-lg"></i>
            </a>
            <a class="nav-link text-white" href="{{ path('app_login') }}">
                <i class="fa-solid fa-right-to-bracket fa-lg"></i>
            </a>
        {% endif %}
    </div>
</nav>


<!-- FOOTER -->
{% block footer %}
    <footer class="bg-dark text-white text-center py-3 d-none d-md-block">
        &copy; SnowTricks {{ "now"|date("Y") }}
    </footer>
{% endblock %}

{% block javascripts %}
    <script src="https://cdn.jsdelivr.net/npm/bootstrap@5.3.3/dist/js/bootstrap.bundle.min.js"></script>
    <script src="https://cdnjs.cloudflare.com/ajax/libs/font-awesome/6.3.0/js/all.min.js"></script>
{% endblock %}

</body>
</html>
